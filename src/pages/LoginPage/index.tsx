import { Link } from "react-router-dom";

export default function LoginPage() {
    return(
        <>
        <button>
            <Link to="/Home">Log in</Link>
        </button>
        </>
    )
}